# Kalman-Filter
Kalman Filter written using Satellite data.

First, will choose which satellite dataset to use for the simulation ( could  implement live tracking extracted from a data base)
Then, setup the matrices needed for calculation of said simulation, along w/ approximate noise values for said variables.
Use matplot lib to plot the simulation, seeing how the Kalman filter is able to predict the actual position, vel, or which desired data point 

Will first write using C++, then Python
